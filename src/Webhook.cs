// Code generated by GetStream internal OpenAPI code generator. DO NOT EDIT.

using System;
using System.Security.Cryptography;
using System.Text;
using System.Text.Json;
using StreamChat.Models;

namespace StreamChat
{
    /// <summary>
    /// Webhook event type constants.
    /// </summary>
    public static class WebhookEventType
    {
        public const string AppealAccepted = "appeal.accepted";
        public const string AppealCreated = "appeal.created";
        public const string AppealRejected = "appeal.rejected";
        public const string ExportBulkImageModerationError = "export.bulk_image_moderation.error";
        public const string ExportBulkImageModerationSuccess = "export.bulk_image_moderation.success";
        public const string ExportModerationLogsError = "export.moderation_logs.error";
        public const string ExportModerationLogsSuccess = "export.moderation_logs.success";
        public const string FeedsActivityAdded = "feeds.activity.added";
        public const string FeedsActivityDeleted = "feeds.activity.deleted";
        public const string FeedsActivityFeedback = "feeds.activity.feedback";
        public const string FeedsActivityMarked = "feeds.activity.marked";
        public const string FeedsActivityPinned = "feeds.activity.pinned";
        public const string FeedsActivityReactionAdded = "feeds.activity.reaction.added";
        public const string FeedsActivityReactionDeleted = "feeds.activity.reaction.deleted";
        public const string FeedsActivityReactionUpdated = "feeds.activity.reaction.updated";
        public const string FeedsActivityRemovedFromFeed = "feeds.activity.removed_from_feed";
        public const string FeedsActivityRestored = "feeds.activity.restored";
        public const string FeedsActivityUnpinned = "feeds.activity.unpinned";
        public const string FeedsActivityUpdated = "feeds.activity.updated";
        public const string FeedsBookmarkAdded = "feeds.bookmark.added";
        public const string FeedsBookmarkDeleted = "feeds.bookmark.deleted";
        public const string FeedsBookmarkUpdated = "feeds.bookmark.updated";
        public const string FeedsBookmarkFolderDeleted = "feeds.bookmark_folder.deleted";
        public const string FeedsBookmarkFolderUpdated = "feeds.bookmark_folder.updated";
        public const string FeedsCommentAdded = "feeds.comment.added";
        public const string FeedsCommentDeleted = "feeds.comment.deleted";
        public const string FeedsCommentReactionAdded = "feeds.comment.reaction.added";
        public const string FeedsCommentReactionDeleted = "feeds.comment.reaction.deleted";
        public const string FeedsCommentReactionUpdated = "feeds.comment.reaction.updated";
        public const string FeedsCommentUpdated = "feeds.comment.updated";
        public const string FeedsFeedCreated = "feeds.feed.created";
        public const string FeedsFeedDeleted = "feeds.feed.deleted";
        public const string FeedsFeedUpdated = "feeds.feed.updated";
        public const string FeedsFeedGroupChanged = "feeds.feed_group.changed";
        public const string FeedsFeedGroupDeleted = "feeds.feed_group.deleted";
        public const string FeedsFeedMemberAdded = "feeds.feed_member.added";
        public const string FeedsFeedMemberRemoved = "feeds.feed_member.removed";
        public const string FeedsFeedMemberUpdated = "feeds.feed_member.updated";
        public const string FeedsFollowCreated = "feeds.follow.created";
        public const string FeedsFollowDeleted = "feeds.follow.deleted";
        public const string FeedsFollowUpdated = "feeds.follow.updated";
        public const string FeedsNotificationFeedUpdated = "feeds.notification_feed.updated";
        public const string FeedsStoriesFeedUpdated = "feeds.stories_feed.updated";
        public const string ModerationCustomAction = "moderation.custom_action";
        public const string ModerationFlagged = "moderation.flagged";
        public const string ModerationMarkReviewed = "moderation.mark_reviewed";
        public const string ModerationCheckCompleted = "moderation_check.completed";
        public const string ReviewQueueItemNew = "review_queue_item.new";
        public const string ReviewQueueItemUpdated = "review_queue_item.updated";
        public const string UserBanned = "user.banned";
        public const string UserDeactivated = "user.deactivated";
        public const string UserReactivated = "user.reactivated";
        public const string UserUpdated = "user.updated";
    }

    /// <summary>
    /// Webhook utilities for Stream webhooks.
    /// </summary>
    public static class Webhook
    {
        /// <summary>
        /// Extract the event type from a raw webhook payload.
        /// </summary>
        /// <param name="rawEvent">The raw webhook payload as a byte array</param>
        /// <returns>The event type string (e.g., "message.new"), or null if not found</returns>
        public static string GetEventType(byte[] rawEvent)
        {
            try
            {
                using var doc = JsonDocument.Parse(rawEvent);
                if (doc.RootElement.TryGetProperty("type", out var typeElement))
                {
                    return typeElement.GetString();
                }
                return null;
            }
            catch
            {
                return null;
            }
        }

        /// <summary>
        /// Extract the event type from a raw webhook payload.
        /// </summary>
        /// <param name="rawEvent">The raw webhook payload as a string</param>
        /// <returns>The event type string (e.g., "message.new"), or null if not found</returns>
        public static string GetEventType(string rawEvent)
        {
            return GetEventType(Encoding.UTF8.GetBytes(rawEvent));
        }

        /// <summary>
        /// Deserialize a raw webhook payload into a typed event object.
        /// </summary>
        /// <param name="rawEvent">The raw webhook payload as a byte array</param>
        /// <returns>A typed event object corresponding to the event type</returns>
        /// <exception cref="WebhookException">Thrown if the event type is unknown or deserialization fails</exception>
        public static object ParseWebhookEvent(byte[] rawEvent)
        {
            var eventType = GetEventType(rawEvent);
            if (string.IsNullOrEmpty(eventType))
            {
                throw new WebhookException("Webhook payload missing 'type' field");
            }

            var eventClass = GetEventTypeClass(eventType);
            if (eventClass == null)
            {
                throw new WebhookException($"Unknown webhook event type: {eventType}");
            }

            try
            {
                return JsonSerializer.Deserialize(rawEvent, eventClass);
            }
            catch (Exception ex)
            {
                throw new WebhookException($"Failed to deserialize webhook event: {ex.Message}", ex);
            }
        }

        /// <summary>
        /// Deserialize a raw webhook payload into a typed event object.
        /// </summary>
        /// <param name="rawEvent">The raw webhook payload as a string</param>
        /// <returns>A typed event object corresponding to the event type</returns>
        /// <exception cref="WebhookException">Thrown if the event type is unknown or deserialization fails</exception>
        public static object ParseWebhookEvent(string rawEvent)
        {
            return ParseWebhookEvent(Encoding.UTF8.GetBytes(rawEvent));
        }

        private static Type GetEventTypeClass(string eventType)
        {
            return eventType switch
            {
                "appeal.accepted" => typeof(AppealAcceptedEvent),
                "appeal.created" => typeof(AppealCreatedEvent),
                "appeal.rejected" => typeof(AppealRejectedEvent),
                "export.bulk_image_moderation.error" => typeof(AsyncExportErrorEvent),
                "export.bulk_image_moderation.success" => typeof(AsyncBulkImageModerationEvent),
                "export.moderation_logs.error" => typeof(AsyncExportErrorEvent),
                "export.moderation_logs.success" => typeof(AsyncExportModerationLogsEvent),
                "feeds.activity.added" => typeof(ActivityAddedEvent),
                "feeds.activity.deleted" => typeof(ActivityDeletedEvent),
                "feeds.activity.feedback" => typeof(ActivityFeedbackEvent),
                "feeds.activity.marked" => typeof(ActivityMarkEvent),
                "feeds.activity.pinned" => typeof(ActivityPinnedEvent),
                "feeds.activity.reaction.added" => typeof(ActivityReactionAddedEvent),
                "feeds.activity.reaction.deleted" => typeof(ActivityReactionDeletedEvent),
                "feeds.activity.reaction.updated" => typeof(ActivityReactionUpdatedEvent),
                "feeds.activity.removed_from_feed" => typeof(ActivityRemovedFromFeedEvent),
                "feeds.activity.restored" => typeof(ActivityRestoredEvent),
                "feeds.activity.unpinned" => typeof(ActivityUnpinnedEvent),
                "feeds.activity.updated" => typeof(ActivityUpdatedEvent),
                "feeds.bookmark.added" => typeof(BookmarkAddedEvent),
                "feeds.bookmark.deleted" => typeof(BookmarkDeletedEvent),
                "feeds.bookmark.updated" => typeof(BookmarkUpdatedEvent),
                "feeds.bookmark_folder.deleted" => typeof(BookmarkFolderDeletedEvent),
                "feeds.bookmark_folder.updated" => typeof(BookmarkFolderUpdatedEvent),
                "feeds.comment.added" => typeof(CommentAddedEvent),
                "feeds.comment.deleted" => typeof(CommentDeletedEvent),
                "feeds.comment.reaction.added" => typeof(CommentReactionAddedEvent),
                "feeds.comment.reaction.deleted" => typeof(CommentReactionDeletedEvent),
                "feeds.comment.reaction.updated" => typeof(CommentReactionUpdatedEvent),
                "feeds.comment.updated" => typeof(CommentUpdatedEvent),
                "feeds.feed.created" => typeof(FeedCreatedEvent),
                "feeds.feed.deleted" => typeof(FeedDeletedEvent),
                "feeds.feed.updated" => typeof(FeedUpdatedEvent),
                "feeds.feed_group.changed" => typeof(FeedGroupChangedEvent),
                "feeds.feed_group.deleted" => typeof(FeedGroupDeletedEvent),
                "feeds.feed_member.added" => typeof(FeedMemberAddedEvent),
                "feeds.feed_member.removed" => typeof(FeedMemberRemovedEvent),
                "feeds.feed_member.updated" => typeof(FeedMemberUpdatedEvent),
                "feeds.follow.created" => typeof(FollowCreatedEvent),
                "feeds.follow.deleted" => typeof(FollowDeletedEvent),
                "feeds.follow.updated" => typeof(FollowUpdatedEvent),
                "feeds.notification_feed.updated" => typeof(NotificationFeedUpdatedEvent),
                "feeds.stories_feed.updated" => typeof(StoriesFeedUpdatedEvent),
                "moderation.custom_action" => typeof(ModerationCustomActionEvent),
                "moderation.flagged" => typeof(ModerationFlaggedEvent),
                "moderation.mark_reviewed" => typeof(ModerationMarkReviewedEvent),
                "moderation_check.completed" => typeof(ModerationCheckCompletedEvent),
                "review_queue_item.new" => typeof(ReviewQueueItemNewEvent),
                "review_queue_item.updated" => typeof(ReviewQueueItemUpdatedEvent),
                "user.banned" => typeof(UserBannedEvent),
                "user.deactivated" => typeof(UserDeactivatedEvent),
                "user.reactivated" => typeof(UserReactivatedEvent),
                "user.updated" => typeof(UserUpdatedEvent),
                _ => null
            };
        }

        /// <summary>
        /// Exception thrown when webhook operations fail.
        /// </summary>
        public class WebhookException : Exception
        {
            public WebhookException(string message) : base(message)
            {
            }

            public WebhookException(string message, Exception innerException) : base(message, innerException)
            {
            }
        }

        /// <summary>
        /// Verify the HMAC-SHA256 signature of a webhook payload.
        /// </summary>
        /// <param name="body">The raw request body as a byte array</param>
        /// <param name="signature">The signature from the X-Signature header</param>
        /// <param name="secret">Your webhook secret (found in the Stream Dashboard)</param>
        /// <returns>true if the signature is valid, false otherwise</returns>
        public static bool VerifySignature(byte[] body, string signature, string secret)
        {
            using var hmac = new HMACSHA256(Encoding.UTF8.GetBytes(secret));
            var hash = hmac.ComputeHash(body);
            var expected = Convert.ToHexString(hash).ToLowerInvariant();

#if NET5_0_OR_GREATER
            return CryptographicOperations.FixedTimeEquals(
                Encoding.UTF8.GetBytes(signature),
                Encoding.UTF8.GetBytes(expected)
            );
#else
            // Fallback for older .NET versions - constant time comparison
            if (signature.Length != expected.Length)
                return false;

            uint result = 0;
            for (int i = 0; i < signature.Length; i++)
            {
                result |= (uint)(signature[i] ^ expected[i]);
            }
            return result == 0;
#endif
        }

        /// <summary>
        /// Verify the HMAC-SHA256 signature of a webhook payload.
        /// </summary>
        /// <param name="body">The raw request body as a string</param>
        /// <param name="signature">The signature from the X-Signature header</param>
        /// <param name="secret">Your webhook secret (found in the Stream Dashboard)</param>
        /// <returns>true if the signature is valid, false otherwise</returns>
        public static bool VerifySignature(string body, string signature, string secret)
        {
            return VerifySignature(Encoding.UTF8.GetBytes(body), signature, secret);
        }
    }
}
